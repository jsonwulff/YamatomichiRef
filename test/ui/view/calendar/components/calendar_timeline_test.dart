import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:app/ui/views/calendar/components/calendar_timeline.dart';

main() {
  group('isInList', () {
    var list = [
      DateTime(2021, 01, 01, 0, 0, 0),
      DateTime(2021, 01, 02, 0, 0, 0)
    ];
    test('given an element that is in the list, isInList return true', () {
      //hvordan skal jeg kalde på isInList metoden
    });
  });
}
